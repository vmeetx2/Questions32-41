#include <stdio.h>
#include <math.h>

//done using O root n

int main()
{
    int n, i, flag = 0;
    scanf("%d", &n);
    if (n < 2) flag = 1;

    for (i = 2; i*i <= n && !flag; i++)
        if (n % i == 0) flag = 1;
    printf("%s\n", flag ? "Not prime" : "Prime");
    return 0;
}
