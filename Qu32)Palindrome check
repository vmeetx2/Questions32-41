#include <stdio.h>
int main() {
    int n, temp, rev = 0;
    scanf("%d", &n);
    temp = n;
    while (temp) {
        rev = rev * 10 + temp % 10;
        temp /= 10;
    }
printf("%s\n", rev == n ? "Palindrome" : "Not palindrome");
return 0;
}
