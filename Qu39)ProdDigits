#include <stdio.h>
int main() 
{
    int n,prod=1,digit,flag=0;
    scanf("%d",&n);

    while(n)

{
        digit=n%10;
        if(digit%2){ prod*=digit; flag=1; }
        n/=10;
    }
    printf("%d\n", flag?prod:1);
    return 0;
}
